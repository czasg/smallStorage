训练机器学习模型来识别照片中的对象。 比如：输入数千张有猫或无猫的图像，到机器学习算法中，以获取一个能够判断照片中是否有猫的模型
故在生成的模型中，输入将是一张数字照片，而输出将是一个布尔值

在上述情况中，机器学吸模型就是一个将多维像素值映射到二进制的函数。 找出数百万像素和布尔值之间是否存在潜在的映射关系的过程，就是机器学习

更具体的说，输入可以表示为矩阵M，尺寸为HxW，矩阵中的每一个元素都是照片中每一个像素的灰度值，也就是介于0-255之间的整数
用来表示颜色的强度。而模型的预期输出为一个二进制值，